%h1= @doc.title
%p= @doc.content

= link_to "All docs", docs_path, :class => "btn btn-primary"
= link_to "Fix doc", edit_doc_path(@doc), :class => "btn btn-warning"
= link_to "Trash it", doc_path(@doc), method: :delete, data: { confirm: "Are you sure?" }, :class => "btn btn-danger"